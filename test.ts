const userName: string = 'Student';
const age: number = 15;
const isActive: boolean = true;

const numbers: number[]= [10, 12, 1];
numbers.push(5)

type Track={
    name: string,
    rating: number,
    isPopular: boolean
}

const track ={
    name: 'Track1',
    rating: 3,
    isPopular: true
}